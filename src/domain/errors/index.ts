import { MathematicalError } from '@domain/errors/maths/index';
import { AbstractError } from '@domain/errors/AbstractError';
import { DateTimeError } from '@domain/errors/dateTime';
import { AuthError } from '@domain/errors/auth/authError';

export {
  AbstractError, MathematicalError, DateTimeError, AuthError,
};
