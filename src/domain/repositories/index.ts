import { IAuthRepository } from '@domain/repositories/auth/authRepository';
import { IPictureRepository } from '@domain/repositories/picture/IPictureRepository';

export type { IAuthRepository, IPictureRepository };
